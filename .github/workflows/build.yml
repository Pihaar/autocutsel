name: Build

on:
  push:
    branches: [master]
    tags: ['v*']
  pull_request:
    branches: [master]

jobs:
  version:
    name: Check version consistency
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.get.outputs.version }}
    steps:
      - uses: actions/checkout@v6

      - name: Extract version from configure.ac
        id: get
        run: |
          ver=$(sed -n 's/^AC_INIT(\[autocutsel\],\[\([^]]*\)\].*/\1/p' configure.ac)
          echo "version=$ver" >> "$GITHUB_OUTPUT"
          echo "Version: $ver"

      - name: Verify packaging versions match
        env:
          VER: ${{ steps.get.outputs.version }}
        run: |
          ver="${VER}"
          fail=0
          check() {
            if [ "$2" != "$ver" ]; then
              echo "MISMATCH in $1: got '$2', expected '$ver'"
              fail=1
            else
              echo "OK: $1 = $2"
            fi
          }
          check "autocutsel.spec" "$(sed -n 's/^Version:[[:space:]]*//p' contrib/rpm/autocutsel.spec)"
          check "PKGBUILD" "$(sed -n 's/^pkgver=//p' contrib/arch/PKGBUILD)"
          check "debian/changelog" "$(head -1 debian/changelog | sed 's/.*(\(.*\)-.*/\1/')"
          exit $fail

  build-fedora:
    name: Build (Fedora)
    runs-on: ubuntu-latest
    container: fedora:latest
    steps:
      - name: Install dependencies
        run: |
          dnf install -y \
            gcc make autoconf automake libtool file \
            libX11-devel libXt-devel libXmu-devel libXaw-devel libXext-devel \
            libinput-devel systemd-devel \
            xorg-x11-server-Xvfb xclip

      - uses: actions/checkout@v6

      - name: Bootstrap
        run: ./bootstrap

      - name: Configure
        run: ./configure

      - name: Build
        run: make

      - name: Test
        run: make check || { cat test-suite.log; exit 1; }

  build-opensuse:
    name: Build (openSUSE)
    runs-on: ubuntu-latest
    container: opensuse/tumbleweed:latest
    steps:
      - name: Install dependencies
        run: |
          zypper --non-interactive --force-resolution install \
            gcc make gawk autoconf automake libtool pkg-config file \
            libX11-devel libXt-devel libXmu-devel libXaw-devel libXext-devel \
            libinput-devel libudev-devel \
            xorg-x11-server-Xvfb xclip

      - uses: actions/checkout@v6

      - name: Bootstrap
        run: ./bootstrap

      - name: Configure
        run: ./configure

      - name: Build
        run: make

      - name: Test
        run: make check || { cat test-suite.log; exit 1; }

  build-ubuntu:
    name: Build (Ubuntu)
    runs-on: ubuntu-latest
    container: ubuntu:latest
    steps:
      - name: Install dependencies
        run: |
          apt-get update
          apt-get install -y \
            gcc make autoconf automake libtool pkg-config \
            libx11-dev libxt-dev libxmu-dev libxaw7-dev libxext-dev \
            libinput-dev libudev-dev \
            xvfb xclip

      - uses: actions/checkout@v6

      - name: Bootstrap
        run: ./bootstrap

      - name: Configure
        run: ./configure

      - name: Build
        run: make

      - name: Test
        run: make check || { cat test-suite.log; exit 1; }

  build-debian:
    name: Build (Debian)
    runs-on: ubuntu-latest
    container: debian:stable
    steps:
      - name: Install dependencies
        run: |
          apt-get update
          apt-get install -y \
            gcc make autoconf automake libtool pkg-config \
            libx11-dev libxt-dev libxmu-dev libxaw7-dev libxext-dev \
            libinput-dev libudev-dev \
            xvfb xclip

      - uses: actions/checkout@v6

      - name: Bootstrap
        run: ./bootstrap

      - name: Configure
        run: ./configure

      - name: Build
        run: make

      - name: Test
        run: make check || { cat test-suite.log; exit 1; }

  build-archlinux:
    name: Build (Arch Linux)
    runs-on: ubuntu-latest
    container: archlinux:base-devel
    steps:
      - name: Install dependencies
        run: |
          pacman -Syu --noconfirm \
            autoconf automake libtool pkg-config \
            libx11 libxt libxmu libxaw libxext \
            libinput systemd \
            xorg-server-xvfb xclip

      - uses: actions/checkout@v6

      - name: Bootstrap
        run: ./bootstrap

      - name: Configure
        run: ./configure

      - name: Build
        run: make

      - name: Test
        run: make check || { cat test-suite.log; exit 1; }

  sanitizers:
    name: Sanitizers (Fedora)
    runs-on: ubuntu-latest
    container: fedora:latest
    steps:
      - name: Install dependencies
        run: |
          dnf install -y \
            gcc make autoconf automake libtool file \
            libX11-devel libXt-devel libXmu-devel libXaw-devel libXext-devel \
            libinput-devel systemd-devel \
            xorg-x11-server-Xvfb xclip libasan libubsan

      - uses: actions/checkout@v6

      - name: Bootstrap
        run: ./bootstrap

      - name: Configure (ASan + UBSan)
        run: ./configure CFLAGS="-g -fsanitize=address,undefined -fno-omit-frame-pointer" LDFLAGS="-fsanitize=address,undefined"

      - name: Build
        run: make

      - name: Test
        run: make check || { cat test-suite.log; exit 1; }

  valgrind:
    name: Valgrind (Fedora)
    runs-on: ubuntu-latest
    container: fedora:latest
    steps:
      - name: Install dependencies
        run: |
          dnf install -y \
            gcc make autoconf automake libtool file valgrind \
            libX11-devel libXt-devel libXmu-devel libXaw-devel libXext-devel \
            libinput-devel systemd-devel \
            xorg-x11-server-Xvfb xclip

      - uses: actions/checkout@v6

      - name: Bootstrap
        run: ./bootstrap

      - name: Configure
        run: ./configure CFLAGS="-g -O1 -U_FORTIFY_SOURCE"

      - name: Build
        run: make

      - name: Build test binary
        run: make tests/test-unit

      - name: Valgrind unit tests
        run: valgrind --error-exitcode=1 --leak-check=full --show-leak-kinds=definite ./tests/test-unit

  rpm-fedora:
    name: RPM (Fedora)
    runs-on: ubuntu-latest
    container: fedora:latest
    needs: [version, build-fedora]
    steps:
      - name: Install dependencies
        run: |
          dnf install -y \
            gcc make autoconf automake libtool rpm-build \
            libX11-devel libXt-devel libXmu-devel libXaw-devel libXext-devel \
            libinput-devel systemd-devel

      - uses: actions/checkout@v6

      - name: Create source tarball
        env:
          VER: ${{ needs.version.outputs.version }}
        run: |
          ver="${VER}"
          topdir=$(rpm --eval '%{_topdir}')
          mkdir -p "$topdir/SOURCES"
          tar czf "$topdir/SOURCES/autocutsel-${ver}.tar.gz" \
            --transform "s,^,autocutsel-${ver}/," \
            --exclude=.git *

      - name: Build RPM
        run: rpmbuild -ba contrib/rpm/autocutsel.spec

      - name: Upload RPM
        uses: actions/upload-artifact@v7
        with:
          name: rpm-fedora
          path: ~/rpmbuild/RPMS/**/*.rpm

  rpm-opensuse:
    name: RPM (openSUSE)
    runs-on: ubuntu-latest
    container: opensuse/tumbleweed:latest
    needs: [version, build-opensuse]
    steps:
      - name: Install dependencies
        run: |
          zypper --non-interactive --force-resolution install \
            gcc make gawk autoconf automake libtool rpm-build pkg-config \
            libX11-devel libXt-devel libXmu-devel libXaw-devel libXext-devel \
            libinput-devel libudev-devel \
            xorg-x11-server-Xvfb xclip

      - uses: actions/checkout@v6

      - name: Create source tarball
        env:
          VER: ${{ needs.version.outputs.version }}
        run: |
          ver="${VER}"
          topdir=$(rpm --eval '%{_topdir}')
          mkdir -p "$topdir/SOURCES"
          tar czf "$topdir/SOURCES/autocutsel-${ver}.tar.gz" \
            --transform "s,^,autocutsel-${ver}/," \
            --exclude=.git *

      - name: Build RPM
        run: rpmbuild -ba contrib/rpm/autocutsel.spec

      - name: Rename RPMs for clarity
        run: |
          for f in /usr/src/packages/RPMS/**/*.rpm; do
            dir=$(dirname "$f")
            base=$(basename "$f" .rpm)
            mv "$f" "$dir/${base}.opensuse.rpm"
          done

      - name: Upload RPM
        uses: actions/upload-artifact@v7
        with:
          name: rpm-opensuse
          path: /usr/src/packages/RPMS/**/*.rpm

  deb-debian:
    name: DEB (Debian)
    runs-on: ubuntu-latest
    container: debian:stable
    needs: build-debian
    steps:
      - name: Install dependencies
        run: |
          apt-get update
          apt-get install -y \
            gcc make autoconf automake libtool pkg-config \
            libx11-dev libxt-dev libxmu-dev libxaw7-dev libxext-dev \
            libinput-dev libudev-dev \
            xvfb xclip \
            debhelper fakeroot dpkg-dev

      - uses: actions/checkout@v6

      - name: Build DEB
        run: dpkg-buildpackage -us -uc -b

      - name: Collect DEB
        run: cp ../*.deb .

      - name: Upload DEB
        uses: actions/upload-artifact@v7
        with:
          name: deb-debian
          path: '*.deb'

  pkg-archlinux:
    name: PKG (Arch Linux)
    runs-on: ubuntu-latest
    container: archlinux:base-devel
    needs: [version, build-archlinux]
    steps:
      - name: Install dependencies
        run: |
          pacman -Syu --noconfirm \
            autoconf automake libtool pkg-config \
            libx11 libxt libxmu libxaw libxext \
            libinput systemd \
            xorg-server-xvfb xclip git

      - name: Create build user
        run: |
          useradd -m builder
          echo 'builder ALL=(ALL) NOPASSWD: ALL' >> /etc/sudoers

      - uses: actions/checkout@v6

      - name: Create source tarball
        env:
          VER: ${{ needs.version.outputs.version }}
        run: |
          ver="${VER}"
          tar czf "autocutsel-${ver}.tar.gz" \
            --transform "s,^,autocutsel-${ver}/," \
            --exclude=.git *

      - name: Build package
        run: |
          cp contrib/arch/PKGBUILD .
          chown -R builder:builder .
          su builder -c 'makepkg -sf --noconfirm'

      - name: Upload PKG
        uses: actions/upload-artifact@v7
        with:
          name: pkg-archlinux
          path: '*.pkg.tar.zst'

  release-draft:
    name: Create draft release
    if: startsWith(github.ref, 'refs/tags/')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6

      - name: Create draft release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          tag="${GITHUB_REF#refs/tags/}"
          gh release create "$tag" --draft --generate-notes --title "$tag"

  release:
    name: Upload release assets
    if: startsWith(github.ref, 'refs/tags/')
    runs-on: ubuntu-latest
    needs: [release-draft, rpm-fedora, rpm-opensuse, deb-debian, pkg-archlinux]
    permissions:
      contents: write
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v7

      - name: Upload assets to release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          tag="${GITHUB_REF#refs/tags/}"
          find . -type f \( -name '*.rpm' -o -name '*.deb' -o -name '*.pkg.tar.zst' \) \
            ! -name '*-debug*' ! -name '*-dbgsym*' ! -name '*.src.rpm' \
            -exec gh release upload "$tag" {} --repo "$GITHUB_REPOSITORY" --clobber \;

      - name: Publish release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          tag="${GITHUB_REF#refs/tags/}"
          gh release edit "$tag" --draft=false --latest --repo "$GITHUB_REPOSITORY"

      - name: Trigger OBS stable build
        env:
          OBS_TOKEN: ${{ secrets.OBS_RUNSERVICE_TOKEN }}
        run: |
          curl -sf --max-time 600 -X POST \
            -H "Authorization: Token $OBS_TOKEN" \
            "https://build.opensuse.org/trigger/runservice?project=home:Pihaar:autocutsel&package=autocutsel"
          echo "OBS stable build triggered"
