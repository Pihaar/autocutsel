workflow:
  steps:
    - trigger_services:
        project: home:Pihaar:autocutsel
        package: autocutsel-nightly
  filters:
    event: push
    branches:
      only:
        - master
---
workflow:
  steps:
    - trigger_services:
        project: home:Pihaar:autocutsel
        package: autocutsel
  filters:
    event: tag_push
